import {CollectionSchema} from "../../shared/database/firestore-utils";

/** Collection reference for firestore. */
export const ROOT_COLLECTION_NAME = "account-roots";

/** Validation schema for the roots collection. */
export const ROOT_COLLECTION_SCHEMA: CollectionSchema = {};
